import React from 'react';
// 함수형 컴포넌트 사용하기
function R030_FunctionComponent(props) {
    let {contents} = props

  return <div>
      <h2>{contents}</h2>
  </div>;
}

export default R030_FunctionComponent;
